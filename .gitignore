# Dependencias
node_modules/

# Builds de escritorio (electron-builder) y build web inlineado
dist/
dist-web/
out/
release/

# Steam
steam_appid.txt

# Logs y temporales
*.log
.DS_Store

# Artefacto de build: lo regenera esbuild desde src/game.js (npm run build:game,
# automatico en npm start / build:web / dist). No versionarlo evita ruido en cada diff.
src/game.bundle.js
.venv-art/

# capturas de QA del carril 3D (PLAN_MEJORAS_3D) — se miran y se tiran, no van al repo
capturas-3d/
